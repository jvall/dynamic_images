<%= form_for(@photo) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= label_tag(:name_tag, "Image Name: ") %> <%= text_field_tag(:name_tag) %></p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
